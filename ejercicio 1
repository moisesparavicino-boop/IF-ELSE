using UnityEngine;

public class NewMonoBehaviourScript : MonoBehaviour
{
    int Life = 50;
    
    void Start()
    {
        if (Life >= 30)
            Debug.Log("mostrar herido");
        if (Life <= 0)
            Debug.Log(" mostrar muerto");
        if (Life > 30)
            Debug.Log("mostrar saludable");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
