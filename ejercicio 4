using UnityEngine;

public class NewMonoBehaviourScript3 : MonoBehaviour
{
    int speed = 50;
    void Start()
    {
                if (speed == 0)
        {
            Debug.Log("Detenido");
        }
        else if (speed < 10)
        {
            Debug.Log("caminando");
        }
        else if (speed < 20)
        {
            Debug.Log("corriendo");
        }
        else if (speed >= 20)
        {
            Debug.Log("sprint");
        }


    // Update is called once per frame
    void Update()
    {
        
    }
}
