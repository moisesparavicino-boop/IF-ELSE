using Unity.VisualScripting;
using UnityEngine;

public class NewMonoBehaviourScript2 : MonoBehaviour
{
   
    int level;
    void Start()
    {
        if (level >= 10)
            Debug.Log("mostrar accesoPermitido");
         else
        {
            Debug.Log("mostrar acceso denegado + seguir entrenando");
        }
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
